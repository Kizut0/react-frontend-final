export default function About() {
  return (
    <div className="page-shell">
      <section className="panel">
        <h2 className="page-title">About</h2>
        <p className="section-copy">
          This interface is built on React and connects to the Next.js backend for authentication,
          book CRUD, and borrow management against MongoDB.
        </p>
      </section>
    </div>
  );
}
